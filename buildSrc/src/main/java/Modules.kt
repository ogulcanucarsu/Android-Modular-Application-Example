object Modules {

    // App
    const val app = ":app"

    // Directory
    const val base = ":base"

    //Base
    const val baseData = "$base:base_data"
    const val baseDomain = "$base:base_domain"
    const val basePresentation = "$base:base_presentation"
    const val baseNavigation = "$base:base_navigation"

}